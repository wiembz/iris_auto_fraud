# Inventaire des tables et colonnes GEO

| Schema | Table | Type probable | Colonnes GEO detectees | Cles possibles | Dependances | Commentaire |
|---|---|---|---|---|---|---|
| `staging` | `stg_sinistres` | staging | `REGSINI`/`regsini`, `GOUVSINI`/`gouvsini`, `CITESINI`/`citesini`, `CPOSTSINI`/`cpostsini` | `NUMSNT`, `NUMSNT + GRNTSINI`, `source_geo_key` derivee | `load_dim_geo.py`, `load_fact_sinistre.py` | Source principale de la geographie sinistre. Les valeurs sont issues de saisies manuelles et doivent etre normalisees avant resolution. |
| `staging` | `stg_clients` | staging | `ADR1`, `CPOST`, `CITE`, `GOUVERNOR`, equivalents minuscules | `NUMPERS`, `IDCLT` | `load_dim_client.py` | Geographie client, a ne pas melanger avec `dim_geo` sinistre. |
| `staging` | `stg_production` | staging | `NATINT`, `IDINT`, `IDDELEGA` | `NUMCNT`, `NUMCNT + NUMAVT + NUMMAJ` | `load_fact_contrat.py`, `load_dim_contrat.py` | Peut porter une logique intermediaire/agence/delegation, a auditer separement. |
| `staging` | `stg_inspection` | staging | non confirme dans le perimetre GEO actuel | identifiant inspection, immatriculation | loaders STAFIM | Pas de localisation inspection fiable documentee dans le modele actuel. |
| `dwh` | `dim_geo` | dimension | `geo_sk`, `pays`, `region`, `gouvernorat`, `localite`, `adresse_fragment`, `code_postal`, `geo_entity_type`, `geo_quality_level`, `needs_review`, `geo_key` | `geo_sk`, `geo_key` | `load_dim_geo.py`, `fact_sinistre.geo_sinistre_sk` | Dimension geographique des sinistres. Le code actuel contient aussi des champs de classification/revue a confirmer avec la cible metier finale. |
| `dwh` | `fact_sinistre` | fact | `geo_sinistre_sk` | `fact_sinistre_sk`, `sinistre_garantie_key` | `dwh.dim_geo`, `staging.stg_sinistres`, mapping CSV GEO | Le rattachement GEO doit passer par la cle resolue, pas par les champs bruts. |
| `dwh` | `dim_client` | dimension | `adr1`, `cpost`, `cite`, `gouvernor`, `pays`, `localite` | `client_sk`, `idclt` | `staging.stg_clients` | La geographie client reste denormalisee; pas de `geo_sk` dans cette dimension. |
| `dwh` | `dim_sinistre` | dimension | aucune geographie finale attendue | `sinistre_sk`, `numero_sinistre` | `fact_sinistre` | La geographie du sinistre ne doit pas etre stockee ici. |
| `dwh` | `dim_contrat` | dimension | pas de colonne GEO directe detectee | `contrat_sk`, `contrat_key` | `staging.stg_production`, `fact_contrat`, `fact_sinistre` | Utile pour dependances de production, mais pas une table GEO. |
| `dwh` | `fact_contrat` | fact | intermediaire/agence via `intermediaire_sk` possible | grain contrat/version | `dim_intermediaire`, `staging.stg_production` | A auditer si les analyses Power BI croisent agence, region et production. |
| `dwh` | `dim_intermediaire` | dimension | agence/region non confirmees dans cette etape | `intermediaire_sk`, code intermediaire probable | `fact_contrat`, `fact_sinistre` | Table candidate pour audit agence/region si les colonnes existent en base. |
| fichier | `data/quality_reports/dim_geo/dim_geo_source_to_resolved_mapping.csv` | mapping fichier | `source_region`, `source_gouvernorat`, `source_localite`, `source_code_postal`, `resolved_geo_key`, `geo_sk` | `source_geo_key`, `resolved_geo_key` | `load_fact_sinistre.py` | Critique pour expliquer le passage du brut sinistre vers `geo_sinistre_sk`. |
| fichier | `data/reference/dim_geo/*.csv` | reference | `region`, `gouvernorat`, `delegation`, `localite`, `code_postal`, `aliases` | cle reference ou combinaison metier | `load_dim_geo.py`, audits GEO | Ces fichiers portent les corrections metier et doivent rester tracables. |
